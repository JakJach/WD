@model TeacherReviewThesisViewModel
@{
    ViewData["Title"] = "Wirtualny Dziekanat";
}

@*<div class="container-fluid">
    <div>
        <p class="text-dark text-center">Formularz oceny pracy dypmolowej:</p>
    </div>
    <div>
        <p class="text-dark">Tytuł pracy:</p>
        <h3 class="text-dark text-center">@Model.Title</h3>
    </div>
    <div>
        <p class="text-dark">Przygotowane przez:</p>
        <h6 class="text-dark text-center">@(Model.Student.Name + " " + Model.Student.Surname)</h6>
    </div>
</div>
<div class="container-fluid">
    <div>
        <div class="row">
            <label asp-for="Files" class="text-dark col-2 custom-control-label">Pliki pracy:</label>
            <div class="col-10">
                @foreach (string filename in Model.Files)
                {
                    <label class="text-dark">@filename</label>
                    <a class="btn btn-dark text-white" asp-controller="teacher" asp-action="download" asp-route-filename="@filename">Pobierz</a>
                }
            </div>
        </div>
    </div>
</div>
<div class="container-fluid">
    <div>
        <p class="text-dark text-center"><br />Oceń pracę<br /></p>
    </div>
    <form asp-controller="Teacher" asp-action="ReviewThesis" method="post">
        <div class="form-group form-row">
            <label asp-for="Note" class="text-dark col-2 col-form-label">Ocena</label>
            <div class="col-2">
                <input type="text" asp-for="Note" class="form-control border-dark" required />
                <span asp-validation-for="Note" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group form-row">
            <label asp-for="Review" class="text-dark col-2 col-form-label">Opinia</label>
            <div class="col-10">
                <textarea asp-for="Review" class="form-control border-dark" required></textarea>
                <span asp-validation-for="Review" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group align-content-center">
            <input type="submit" name="submit" class="btn btn-dark" value="Zatwierdź" />
        </div>
    </form>
</div>*@